# Stellar Descent Environment Configuration
# Copy this file to .env for local development

# App title shown in browser tab
VITE_APP_TITLE=STELLAR DESCENT: PROXIMA BREACH

# Base path for assets (leave empty for root)
# VITE_PUBLIC_PATH=/stellar-descent/

# ============================================================================
# Build Flags - Feature toggles for dev vs production builds
# ============================================================================

# Enable dev menu (backtick key to toggle)
# Shows level jumping, god mode, noclip, and other dev tools
# VITE_DEV_MENU=true

# Unlock all campaign levels without completing previous missions
# Useful for testing specific levels without playing through
# VITE_UNLOCK_ALL_CAMPAIGNS=true

# Enable AI Player option in Settings > Gameplay
# Allows automated playtesting and demos
# VITE_ENABLE_AI_PLAYER=true

# Show debug info in HUD (FPS, entity counts, etc.)
# VITE_DEBUG_HUD=true

# ============================================================================
# Notes:
# - For development, use .env.development (already configured with flags enabled)
# - For production builds, use .env.production (flags disabled by default)
# - See src/game/core/BuildConfig.ts for how these flags are used
# ============================================================================
